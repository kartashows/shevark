version: '3.8'

services:
  postgres:
    image: postgres:latest
    volumes:
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql

  bot:
    build: .
    depends_on:
      - postgres
    volumes:
      - .:/app
    command: python3 main.py
